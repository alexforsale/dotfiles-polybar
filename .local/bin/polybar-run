#!/bin/sh

case ${DESKTOP_SESSION} in
    i3)
        # i3 set polybar top
        rofi -no-lazy-grab -show drun \
            -theme themes/i3-appmenu.rasi
        ;;
    openbox)
        # Launch Rofi
        rofi -no-lazy-grab -show drun \
            -theme themes/openbox-appmenu.rasi
        ;;
esac
